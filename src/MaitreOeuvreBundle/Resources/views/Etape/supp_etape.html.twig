{% extends "MaitreOeuvreBundle::layout.html.twig" %}

{% block contents %}
    {{ parent() }}
    {% block body %}
        {{ parent() }}

        {{ form_start(form) }}
        <p>Etes-vous certain de vouloir supprimer cette étape ?</p>
        <a href="{{ path('index_etape')}}" class="btn btn-default">Annuler</a>
        {{ form_widget(form.delete, {'attr': {'class': 'btn btn-danger'}}) }}
        {{ form_end(form) }}

    {% endblock %}
{% endblock %}
